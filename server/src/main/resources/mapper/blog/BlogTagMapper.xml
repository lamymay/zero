<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis mapper for BlogTag  Auto generated by fms-mybatis-generator -->
<mapper namespace="com.arc.zero.mapper.blog.BlogTagMapper">
	<resultMap id="blogTag" type="com.arc.core.model.domain.blog.BlogTag">
		<id property="id" column="blog_tag_id" javaType="long" jdbcType="BIGINT" />
		<result property="tagName" column="blog_tag_tag_name" javaType="string" jdbcType="VARCHAR" />
	</resultMap>

	<sql id="sqlBlogTagColumns">
		blog_tag_.id AS blog_tag_id,
		blog_tag_.tag_name AS blog_tag_tag_name
	</sql>

    <delete id="delete" parameterType="long"  >
        DELETE FROM  t_blog_tag
        WHERE  id = #{id,jdbcType=BIGINT}

    </delete>

    <select id="get" parameterType="long" resultMap="blogTag">
		SELECT <include refid="sqlBlogTagColumns" />
        FROM t_blog_tag blog_tag_
        WHERE blog_tag_.id = #{id,jdbcType=BIGINT}
	</select>

    <select id="list"   resultMap="blogTag">
		SELECT <include refid="sqlBlogTagColumns" />
        FROM t_blog_tag blog_tag_
	</select>

	<insert id="save" parameterType="com.arc.core.model.domain.blog.BlogTag" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO t_blog_tag (
			tag_name
		)
        VALUES (
			#{tagName,jdbcType=VARCHAR}
		)
	</insert>

	<update id="update" parameterType="com.arc.core.model.domain.blog.BlogTag">
		UPDATE t_blog_tag
		<set>
			<if test="tagName != null and tagName != ''">tag_name = #{tagName,jdbcType=VARCHAR},</if>
		</set>
		WHERE id = #{id,jdbcType=BIGINT}
	</update>

</mapper>
